# pnpm 11 aborts scripts when a native transitive dep (unrs-resolver, via
# eslint-config-next) has an unapproved build script — that blocked `pnpm lint`
# from ever running the linter. The build is not needed for linting.
verify-deps-before-run=false
