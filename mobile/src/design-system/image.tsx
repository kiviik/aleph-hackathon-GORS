// expo-image, never react-native's Image: memory-efficient caching, placeholders and a
// `recyclingKey` that matters as soon as an image lands inside a recycled list row.
export { Image, type ImageProps } from "expo-image";
