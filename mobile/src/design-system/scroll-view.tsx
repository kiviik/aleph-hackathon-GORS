// Only for content that genuinely is not a list (a form, a single card stack).
// Anything backed by an array goes through `List` instead.
export { ScrollView, type ScrollViewProps } from "react-native";
