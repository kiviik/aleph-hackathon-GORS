// Single source for the tenant brand shown in the shell.
// Swap this for the connected brand (or wire it to the engine API later).
// Deliberately NOT "Meridian" — that was only the prototype's sample brand.
export const BRAND = "Complot";
export const BRAND_INITIAL = BRAND.charAt(0).toUpperCase();
export const BRAND_SEGMENTS = "MUJER · TEEN";
